<Window x:Class="GeminiLiveShare.App.Views.BrowserAgentDebugPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Browser Agent Debug"
        Width="620"
        Height="420"
        MinWidth="480"
        MinHeight="300"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Width="150"
                Height="34"
                Content="Get active page"
                Click="OnGetActivePageClick" />
            <Button Width="150"
                Height="34"
                Margin="10,0,0,0"
                Content="Get form fields"
                Click="OnGetFormFieldsClick" />
        </StackPanel>
        <TextBox x:Name="ResultBox"
                 Grid.Row="2"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="13" />
    </Grid>
</Window>
